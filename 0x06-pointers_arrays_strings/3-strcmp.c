#include "main.h"
#include <string.h>

/**
 * _strcmp - adding two lines together
 * @s1: first string
 * @s2: 2nd string
 * Return: result
 */

int _strcmp(char *s1, char *s2)
{
	int i = strcmp(s1, s2);

	return (i);
}
